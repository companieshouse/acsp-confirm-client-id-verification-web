{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach to Remote",
            "address": "localhost",
            "port": 9242,
            "restart": true,
            "sourceMaps": true,
            "localRoot": "${workspaceFolder}/src",
            "remoteRoot": "/app/src",
            "resolveSourceMapLocations": [
                "${workspaceFolder}/**",
                "!**/node_modules/**"
            ]
        }
    ]
}