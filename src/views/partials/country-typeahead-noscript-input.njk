{% from "govuk/components/select/macro.njk" import govukSelect %}
    {% set DROPDOWN = [
      { value: '', text: dropdownDefaultText }
    ] %}

    {% set dropdownArray = validCountryArray %}

    {% for i in dropdownArray %}
      {% set dropdownObject = { value: i, text: i, selected: dropdownValue === i } %}
      {% set DROPDOWN = DROPDOWN.concat(dropdownObject) %}
    {% endfor %}


    {{ govukSelect({
      label: {
        html: dropdownTitle
      },
      id: "countryInput_" +  index,
      name: "countryInput_" +  index,
      value: dropdownValue,
      errorMessage: {
               text: errors["countryInput_" + index].text
      }if errors["countryInput_" + index],
      items: DROPDOWN
    }) }}

